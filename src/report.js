import glob from 'fast-glob';
import { readdir, readFile } from 'fs/promises';

const loadReport = async (f) => {
  await readFile
};

export const report = (din, out) => {
  const reports = await glob('*/*/report.json', { cwd: din });
  const Promise.all(map(reports, async (p_) => {
    const path
  }));
};
